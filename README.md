## Group's members scrapper

### Instructions:
 
 - add your `ApiId`, `ApiHash` and `Phone number` to constants
 - add `title of channel` which participants you want to parse 
 > `!important`: you should be an admin of this channel